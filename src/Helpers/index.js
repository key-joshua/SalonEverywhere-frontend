import * as backendURLs from './backendURLs';
import { validateEmailForm, validateFormOne, validateFormTwo, validateFormThree } from './validate';

export { validateEmailForm, validateFormOne, validateFormTwo, validateFormThree, backendURLs };
